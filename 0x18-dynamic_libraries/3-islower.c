#include "main.h"

/**
 * _islower - program that checks for lowercase
 * @c: the character to check
 * Return:1 for lowercase and 0 othrwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
