#include "main.h"

/**
 * _isupper - a function to print uppercase letters
 * @c:the character input
 * Return: 1 if its uppercase and 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
