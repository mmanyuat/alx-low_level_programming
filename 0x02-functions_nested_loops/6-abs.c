#include <stdio.h>

/**
 * _abs - function to find the absolute value of an integer
 * @r: the number
 * Return: the absolute value
 */

int _abs(int r)
{
	if (r < 0)
	{
	return (-r);
	}
	return (r);
}
