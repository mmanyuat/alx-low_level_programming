#include "main.h"

/**
 * _islower - look for lowercase alphabets
 * @c: the character to be screened
 * Return:1 for lowercase 0 otherwise
 *
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
